To run the backend:
>> mvn spring-boot:run
or
>> mvn clean package
Then
>> java -jar target/webAppDemo-0.0.1-SNAPSHOT.jar

To run the frontend:
>> cd src/main/webApp
Then
>> npm install
>> npm start

To login, the credentials are user/password.
